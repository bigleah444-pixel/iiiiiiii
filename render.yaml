databases:
  - name: mort
    ipAllowList: []
    region: frankfurt
    plan: free

services:
  - type: web
    plan: free
    name: mort
    env: python
    buildCommand: pip install -U -r requirements.txt
    startCommand: python3 kr.py
    region: frankfurt
    envVars:
      - key: TOKEN 
        sync: false
      - key: DATABASE_URL
        fromDatabase:
          name: mort 
          property: connectionString
    autoDeploy: false
